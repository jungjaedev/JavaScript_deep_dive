// 첫번재종이컵을 뒤집는다
// 있으면 먹는다
// 없으면 다음 종이컵을 뒤집는다
// 종이컵 수만큼 반복 

function multiplyBy2(input) {
  input = input * 2;
  return input;
}

function divideBy2(input) {
  input = input / 2;
  return input;
}

console.log(multiplyBy2(6));
console.log(divideBy2(6));